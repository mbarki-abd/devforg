# ============================================
# DevForge Environment Configuration
# Copy to .env and update values
# ============================================

# Application
APP_KEY=your-32-character-secret-key-here
NODE_ENV=production
LOG_LEVEL=info

# Database (PostgreSQL)
DB_DATABASE=devforge
DB_USER=devforge
DB_PASSWORD=your-secure-database-password
DB_TIMEZONE=UTC

# Redis
REDIS_URL=redis://redis:6379

# Agent API Authentication
# Generate with: openssl rand -hex 32
AGENT_API_KEY=your-secure-api-key-for-agent-communication

# Credential Encryption Key (32 characters)
# Generate with: openssl rand -hex 16
CREDENTIAL_ENCRYPTION_KEY=your-32-char-encryption-key

# ============================================
# Optional: Cloud Provider Credentials
# ============================================

# Anthropic (Claude AI)
ANTHROPIC_API_KEY=

# Azure Service Principal
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_TENANT_ID=
AZURE_SUBSCRIPTION_ID=

# Google Cloud
# Mount service account JSON to /app/credentials/gcloud-key.json
GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/gcloud-key.json

# ============================================
# Optional: External Services
# ============================================

# SMTP (for email notifications)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@devforge.ilinqsoft.com

# ============================================
# GitHub Actions Secrets Required
# ============================================
# SSH_PRIVATE_KEY     - SSH key for server access
# SERVER_HOST         - Server hostname/IP
# SERVER_USER         - SSH username
# APP_KEY             - Application secret key
# DB_DATABASE         - Database name
# DB_USER             - Database username
# DB_PASSWORD         - Database password
# AGENT_API_KEY       - API authentication key
# CREDENTIAL_ENCRYPTION_KEY - Encryption key
# ANTHROPIC_API_KEY   - (optional) Claude AI key
# AZURE_CLIENT_ID     - (optional) Azure credentials
# AZURE_CLIENT_SECRET - (optional) Azure credentials
# AZURE_TENANT_ID     - (optional) Azure credentials
